diff -rpN -U 2 a/configure.ac b/configure.ac
--- a/configure.ac	2015-11-26 15:35:36.000000000 -0500
+++ b/configure.ac	2015-12-24 02:26:40.774746393 -0500
@@ -225,4 +225,15 @@ fi
 AC_SUBST(CINNAMON_SYSTEM_CA_FILE,["$with_ca_certificates"])
 
+AC_ARG_ENABLE(notification-server,
+  AS_HELP_STRING([--disable-notification-server],[Disable built-in notification server]),,enable_notification_server=no)
+
+if test "x$enable_notification_server" = "xyes"; then
+  AC_DEFINE(HAVE_NOTIFICATION_SERVER, [1], [Define if we have NotificationDaemon])
+  AC_SUBST(HAVE_NOTIFICATION_SERVER, [1])
+else
+  AC_SUBST(HAVE_NOTIFICATION_SERVER, [0])
+fi
+AM_CONDITIONAL(HAVE_NOTIFICATION_SERVER, [test "x$enable_notification_server" = xyes])
+
 GTK_DOC_CHECK([1.15], [--flavour no-tmpl])
 AC_CONFIG_FILES([
diff -rpN -U 2 a/js/Makefile.am b/js/Makefile.am
--- a/js/Makefile.am	2015-11-26 15:35:36.000000000 -0500
+++ b/js/Makefile.am	2015-12-24 02:27:14.929598862 -0500
@@ -60,5 +60,4 @@ nobase_dist_js_DATA = \
 	ui/cinnamonMountOperation.js \
 	ui/osdWindow.js \
-	ui/notificationDaemon.js \
 	ui/overview.js		\
 	ui/overrides.js \
@@ -87,2 +86,7 @@ nobase_dist_js_DATA = \
 	ui/workspacesView.js	\
 	ui/xdndHandler.js
+
+if HAVE_NOTIFICATION_SERVER
+nobase_dist_js_DATA += ui/notificationDaemon.js
+endif
+
diff -rpN -U 2 a/js/misc/config.js.in b/js/misc/config.js.in
--- a/js/misc/config.js.in	2015-11-26 15:35:36.000000000 -0500
+++ b/js/misc/config.js.in	2015-12-24 02:28:14.765342760 -0500
@@ -11,2 +11,5 @@ const HAVE_BLUETOOTH = @HAVE_BLUETOOTH@;
 /* The system TLS CA list */
 const CINNAMON_SYSTEM_CA_FILE = '@CINNAMON_SYSTEM_CA_FILE@';
+/* 1 if NotificationDaemon is available, 0 otherwise */
+const HAVE_NOTIFICATION_SERVER = @HAVE_NOTIFICATION_SERVER@;
+
diff -rpN -U 2 a/js/ui/main.js b/js/ui/main.js
--- a/js/ui/main.js	2015-11-26 15:35:36.000000000 -0500
+++ b/js/ui/main.js	2015-12-24 02:29:52.756929075 -0500
@@ -76,4 +76,6 @@
  */
 
+const Config = imports.misc.config;
+
 const Clutter = imports.gi.Clutter;
 const Gio = imports.gi.Gio;
@@ -105,5 +107,7 @@ const RunDialog = imports.ui.runDialog;
 const Layout = imports.ui.layout;
 const LookingGlass = imports.ui.lookingGlass;
-const NotificationDaemon = imports.ui.notificationDaemon;
+if(Config.HAVE_NOTIFICATION_SERVER) {
+    const NotificationDaemon = imports.ui.notificationDaemon;
+}
 const WindowAttentionHandler = imports.ui.windowAttentionHandler;
 const Scripting = imports.ui.scripting;
@@ -400,5 +404,7 @@ function start() {
     indicatorManager = new IndicatorManager.IndicatorManager();
     keyboard = new Keyboard.Keyboard();
-    notificationDaemon = new NotificationDaemon.NotificationDaemon();
+    if(Config.HAVE_NOTIFICATION_SERVER) {
+        notificationDaemon = new NotificationDaemon.NotificationDaemon();
+    }
     windowAttentionHandler = new WindowAttentionHandler.WindowAttentionHandler();
 
diff -rpN -U 2 a/src/main.c b/src/main.c
--- a/src/main.c	2015-11-26 15:35:36.000000000 -0500
+++ b/src/main.c	2015-12-24 02:30:37.767741144 -0500
@@ -140,6 +140,8 @@ cinnamon_dbus_init (gboolean replace)
   /* ...and the org.gnome.Magnifier service. */
                             MAGNIFIER_DBUS_SERVICE, FALSE,
+#ifdef HAVE_NOTIFICATION_SERVER
   /* ...and the org.freedesktop.Notifications service. */
                             "org.freedesktop.Notifications", FALSE,
+#endif
                             NULL);
   /* ...and the on-screen keyboard service */
